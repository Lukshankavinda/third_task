import { Request, Response } from 'express';
import { BaseEntity, getRepository } from 'typeorm';
import { counters } from '../Entities/counters';

class counterController extends BaseEntity{

}

export default counterController;